

   <div class="row">
          <% current_user.coffees.each do |coffee| %>
   	   <div class="col-md-4">

            <%= link_to image_tag(url_for(coffee.image),class: "coffee"), coffee_path(coffee)%>
            </div>
       <% end %>

         </div>


        
